{
  "name": "auth0-tenant",
  "version": "1.5.0",
  "private": true,
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "deploy-dev": "a0deploy import -c auth0-config-dev.json -i auth0-tenant",
    "deploy-staging": "a0deploy import -c auth0-config-staging.json -i auth0-tenant",
    "deploy-prod": "a0deploy import -c auth0-config-prod.json -i auth0-tenant",
    "test": "vitest run --silent --coverage",
    "test:watch": "vitest --silent"
  },
  "devDependencies": {
    "@auth0/actions": "^0.9.0",
    "@vitest/coverage-v8": "^4.0.6",
    "auth0-deploy-cli": "^8.19.0",
    "vitest": "~4.0.3",
    "vitest-mock-commonjs": "^1.0.2",
    "vitest-mock-extended": "^3.1.0"
  }
}
